import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-navigation-bar-overlaps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
